# Supply-chain provider credentials. Copy to `.env` (gitignored) and fill in.
# Never commit real secrets — `.env` is ignored; only this template is tracked.

# DigiKey Product Information API v4 (OAuth2 client-credentials)
DIGIKEY_CLIENT_ID=
DIGIKEY_CLIENT_SECRET=

# Nexar / Octopart (optional)
# NEXAR_CLIENT_ID=
# NEXAR_CLIENT_SECRET=
