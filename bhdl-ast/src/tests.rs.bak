// bhdl-ast/src/tests.rs
use crate::{HasName, blocks::*, items::*, common::*, SyntaxNode, SyntaxToken, BhdlLanguage}; // Use SyntaxNode/Token
use crate::items; // Ensure items module is explicitly used for disambiguation if needed
use bhdl_parser::SyntaxKind; // Removed duplicate BhdlLanguage import
use rowan::ast::AstNode as RowanAstNode; 
use crate::{Board, NetDecl, ParamAssign, PinRef, NetRef, ConnectionStmt, PortDecl, common::SimpleIdentRef};
use crate::v2_statements::{PowerDecl, GroundDecl};

/// Parses the source code and returns the root SyntaxNode (SOURCE_FILE).
fn parse_test_text(text: &str) -> SyntaxNode<BhdlLanguage> {
    let parse_result = bhdl_parser::parse(text); // Call the correctly imported function
    // For tests, we assume parsing succeeds and panic otherwise.
    // We also ignore errors for now, focusing on AST structure.
    parse_result.syntax()
}

/// Parses the source code and finds the first AST node of type T.
/// Panics if parsing fails or the node is not found.
fn parse_and_find_node<T: RowanAstNode<Language = BhdlLanguage>>(text: &str) -> T {
    let root_node = parse_test_text(text);
    // Debug print the structure around potential nodes
    println!("--- Debugging AST for type: {} ---", std::any::type_name::<T>());
    println!("Input Text:\n{}", text);
    let target_node = root_node
        .descendants()
        .find(|node| <T as RowanAstNode>::can_cast(node.kind()));

    if let Some(node) = target_node.as_ref() {
        println!("Found potential node ({:?}):\n{:#?}", node.kind(), node);
        if let Some(parent) = node.parent() {
             println!("Parent ({:?}):\n{:#?}", parent.kind(), parent);
        }
        println!("Children:");
        for child in node.children_with_tokens() {
            println!("  - {:?}", child);
        }
    } else {
        println!("Potential node for {} not found!", std::any::type_name::<T>());
        // Print root structure if node not found at all
        println!("Root node structure:\n{:#?}", root_node);
    }
    println!("--- End Debug ---");

    target_node // Use the node found by can_cast
        .and_then(T::cast)
        .expect(&format!(
            "Node {} not found or failed to cast in text:\n{}",
            std::any::type_name::<T>(),
            text
        ))
}

/// Parses the source code and finds the first syntax node of a given kind.
/// Panics if parsing fails or the node is not found.
fn parse_and_find_syntax_node(text: &str, kind: SyntaxKind) -> SyntaxNode<BhdlLanguage> { // Updated return type
    let root_node = parse_test_text(text);
    root_node
        .descendants()
        .find(|node| node.kind() == kind)
        .expect(&format!(
            "SyntaxKind {:?} not found in text:\n{}",
            kind, text
        ))
}


#[test]
fn it_works() {
    // Basic test moved from lib.rs
    assert_eq!(2 + 2, 4);
}

// --- Board Tests ---

#[test]
fn test_parse_empty_board() {
    let board_node = parse_and_find_syntax_node("board EmptyBoard {}", SyntaxKind::BOARD_DEF);
    let board = Board::cast(board_node).expect("Should cast to Board");

    assert!(board.name().is_some());
    assert_eq!(board.name().unwrap().text(), "EmptyBoard");
    // v2.0 boards have power/ground declarations and connections
    assert_eq!(board.power_decls().count(), 0);
    assert_eq!(board.ground_decls().count(), 0);
    assert_eq!(board.connections().count(), 0);
}

#[test]
fn test_parse_v2_board_with_power() {
    let text = r#"
board PowerBoard {
    power VCC = 5V @ 1A;
    ground GND;
}
"#;
    let board = parse_and_find_node::<Board>(text);
    
    assert_eq!(board.name().unwrap().text(), "PowerBoard");
    assert_eq!(board.power_decls().count(), 1);
    assert_eq!(board.ground_decls().count(), 1);
    
    let power = board.power_decls().next().unwrap();
    assert_eq!(power.name().unwrap().text(), "VCC");
}

// TODO: Rewrite this test for v2.0 syntax
// #[test]
// fn test_parse_board_with_blocks() {
//     let text = r#"
// board MyBoard {
//     parameters { param1 = 10; }
//     ports { port IN_PORT: signal; }
//     nets { net NetA: signal; }
// }
// "#;
//     let board = parse_and_find_node::<Board>(text);
// 
//     assert_eq!(board.name().unwrap().text(), "MyBoard");
//     // v2.0 boards don't have these blocks anymore
//     // These tests need to be rewritten for v2.0 syntax
// }


// --- Parameter/Port Declaration Tests ---

// TODO: Update for v2.0 syntax - parameters are now in component instantiation
// #[test]
// fn test_param_assign_name() {
//     let param_assign = parse_and_find_node::<ParamAssign>("board B { parameters { my_param = 5; } }");
//     assert!(param_assign.name().is_some());
//     assert_eq!(param_assign.name().unwrap().text(), "my_param");
//     assert!(param_assign.value().is_some());
// }

// TODO: Update for v2.0 syntax - parameters are now in component instantiation
// #[test]
// fn test_param_assign_with_value() {
//     let param_assign = parse_and_find_node::<ParamAssign>("board B { parameters { WIDTH = 32; } }");
//     assert_eq!(param_assign.name().unwrap().text(), "WIDTH");
//     assert!(param_assign.value().is_some());
//     assert!(param_assign.value_literal_token().is_some());
//     assert_eq!(param_assign.value_literal_token().unwrap().text(), "32");
// }

// --- Net Declaration Tests ---

// TODO: Update for v2.0 syntax - nets are declared differently
// #[test]
// fn test_net_decl() {
//     let net_decl = parse_and_find_node::<NetDecl>(
//         "board B { nets { net ControlSig: signal; } }"
//     );
//     assert_eq!(net_decl.name().unwrap().text(), "ControlSig");
//     assert!(net_decl.type_ref().is_some());
//     assert_eq!(net_decl.type_ref().unwrap().name_token().unwrap().text(), "signal");
// }

// TODO: Update for v2.0 syntax - nets are declared differently
// #[test]
// fn test_net_decl_with_bus_range() {
//      let net_decl = parse_and_find_node::<NetDecl>(
//         "board B { nets { net ADDR[15:0]: signal; } }"
//     );
//     assert_eq!(net_decl.name().unwrap().text(), "ADDR");
//     assert!(net_decl.bus_suffix().is_some());
//     let suffix = net_decl.bus_suffix().unwrap();
//     assert!(suffix.range().is_some());
// }

// --- Pin/Net Reference Tests ---

#[test]
fn test_pin_ref_instance_pin() {
    let pin_ref = parse_and_find_node::<PinRef>(
        "board B { connections { connect U1.Data -> NetA; } }"
    );
    assert_eq!(pin_ref.instance_name().unwrap().text(), "U1");
    assert_eq!(pin_ref.pin_name().unwrap().text(), "Data");
}

#[test]
fn test_pin_ref_with_bus_index() {
    let pin_ref = parse_and_find_node::<PinRef>(
        "board B { connections { connect U1.Data[0] -> NetA; } }"
    );
    assert_eq!(pin_ref.instance_name().unwrap().text(), "U1");
    assert_eq!(pin_ref.pin_name().unwrap().text(), "Data");
    assert!(pin_ref.bus_suffix().is_some());
    let suffix = pin_ref.bus_suffix().unwrap();
    assert!(suffix.index_expr_node().is_some());
    assert_eq!(suffix.index_expr_node().unwrap().text(), "0");
}

// TODO: Update to v2.0 syntax
// #[test]
// fn test_net_ref() {
//     let conn = parse_and_find_node::<ConnectionStmt>(
//         "board B { connections { connect U1.PinA -> TheNet; } }"
//     );
//     let sink_node = conn.sink().expect("Sink node not found");
//     assert!(SimpleIdentRef::cast(sink_node.clone()).is_some()); 
//     assert_eq!(SimpleIdentRef::cast(sink_node).unwrap().name_token().unwrap().text(), "TheNet");
// }

#[test]
fn test_net_ref_with_bus_slice() {
     let net_ref = parse_and_find_node::<NetRef>(
        "board B { connections { connect U1.Byte -> Bus[7:0]; } }"
    );
    assert_eq!(net_ref.name_token().unwrap().text(), "Bus");
    assert!(net_ref.bus_suffix().is_some());
    let suffix = net_ref.bus_suffix().unwrap();
    assert!(suffix.range().is_some());
}

// --- Connection Statement Tests ---

// TODO: Update to v2.0 syntax
// #[test]
// fn test_connection_pin_to_net() {
//     let conn = parse_and_find_node::<ConnectionStmt>(
//         "board B { connections { connect U1.Pin -> NetA; } }"
//     );

//     let source_node = conn.source().unwrap();
//     assert_eq!(source_node.kind(), SyntaxKind::PIN_REF);
//     let source_pin_ref = PinRef::cast(source_node.clone()).unwrap();
//     assert_eq!(source_pin_ref.instance_name().unwrap().text(), "U1");
//     assert_eq!(source_pin_ref.pin_name().unwrap().text(), "Pin");

//     let sink_node = conn.sink().unwrap(); 
//     assert!(SimpleIdentRef::can_cast(sink_node.kind()) || NetRef::can_cast(sink_node.kind()));
//     if let Some(sink_ref) = SimpleIdentRef::cast(sink_node.clone()) {
//         assert_eq!(sink_ref.name_token().unwrap().text(), "NetA");
//     } else if let Some(sink_ref) = NetRef::cast(sink_node) {
//         assert_eq!(sink_ref.name_token().unwrap().text(), "NetA");
//     } else {
//         panic!("Sink node is neither SimpleIdentRef nor NetRef");
//     }
// }

#[test]
fn test_connection_net_to_pin() {
     let conn = parse_and_find_node::<ConnectionStmt>(
        "board B { connections { connect NetA -> U1.Pin; } }"
    );
    let source_node = conn.source().unwrap();
    assert!(SimpleIdentRef::can_cast(source_node.kind()) || NetRef::can_cast(source_node.kind()));
     if let Some(source_ref) = SimpleIdentRef::cast(source_node.clone()) {
        assert_eq!(source_ref.name_token().unwrap().text(), "NetA");
    } else if let Some(source_ref) = NetRef::cast(source_node.clone()) {
        assert_eq!(source_ref.name_token().unwrap().text(), "NetA");
    } else {
        panic!("Source node is neither SimpleIdentRef nor NetRef");
    }

    let sink_node = conn.sink().unwrap();
    assert_eq!(sink_node.kind(), SyntaxKind::PIN_REF);
    let sink_pin_ref = PinRef::cast(sink_node).expect("Should cast sink to PinRef");
    assert_eq!(sink_pin_ref.instance_name().unwrap().text(), "U1");
    assert_eq!(sink_pin_ref.pin_name().unwrap().text(), "Pin");
}

#[test]
fn test_connection_pin_to_pin() {
     let conn = parse_and_find_node::<ConnectionStmt>(
        "board B { connections { connect U1.Out -> U2.In; } }"
    );
    let source_node = conn.source().unwrap();
    assert_eq!(source_node.kind(), SyntaxKind::PIN_REF);
    let source_pin_ref = PinRef::cast(source_node.clone()).unwrap();
    assert_eq!(source_pin_ref.instance_name().unwrap().text(), "U1");
    assert_eq!(source_pin_ref.pin_name().unwrap().text(), "Out");

    let sink_node = conn.sink().unwrap();
    assert_eq!(sink_node.kind(), SyntaxKind::PIN_REF);
    let sink_pin_ref = PinRef::cast(sink_node).expect("Should cast sink to PinRef");
    assert_eq!(sink_pin_ref.instance_name().unwrap().text(), "U2");
    assert_eq!(sink_pin_ref.pin_name().unwrap().text(), "In");
}

// TODO: Add tests for multi-connections (comma separated)
// TODO: Add tests for interface connections (<=>)

// --- Pin Declaration Tests ---

#[test]
fn test_pins_block_iterator() {
    let pins_block = parse_and_find_node::<PinsBlock>(
        "component C { pins { pin P1: signal; pin P2: signal; pin P3: signal; } }"
    );
    let pins: Vec<_> = pins_block.pins().collect();
    assert_eq!(pins.len(), 3);
    assert_eq!(pins[0].name().unwrap().text(), "P1");
    assert_eq!(pins[1].name().unwrap().text(), "P2");
    assert_eq!(pins[2].name().unwrap().text(), "P3");
}

#[test]
fn test_pin_decl_name_direction_type() {
    let pin_decl = parse_and_find_node::<PinDecl>(
        "component C { pins { pin DATA0: signal; } }"
    );
    assert!(pin_decl.name().is_some());
    assert_eq!(pin_decl.name().unwrap().text(), "DATA0");
    assert!(pin_decl.type_ref().is_some());
    assert_eq!(pin_decl.type_ref().unwrap().name_token().unwrap().text(), "signal");
}

#[test]
fn test_pin_decl_minimal() {
     let pin_decl = parse_and_find_node::<PinDecl>(
        "component C { pins { pin RESET_N: signal; } }"
    );
    assert_eq!(pin_decl.name().unwrap().text(), "RESET_N");
    assert!(pin_decl.type_ref().is_some());
    assert_eq!(pin_decl.type_ref().unwrap().name_token().unwrap().text(), "signal");
}

#[test]
fn test_pin_decl_with_bus_range() {
     let pin_decl = parse_and_find_node::<PinDecl>(
        "component C { pins { pin ADDR[31:0]: signal; } }"
    );
    assert_eq!(pin_decl.name().unwrap().text(), "ADDR");
    assert!(pin_decl.bus_suffix().is_some());
    let suffix = pin_decl.bus_suffix().unwrap();
    assert!(suffix.range().is_some());
}

// Add tests for Module, ComponentDef, ComponentInst etc. here later 

#[test]
fn test_typedef_def() {
    let typedef_basic = parse_and_find_node::<TypeDef>("typedef simple_signal { type = signal; voltage = 1.8V; }");
    assert_eq!(typedef_basic.name().unwrap().text(), "simple_signal");
    assert!(typedef_basic.base_type().is_none());
    assert_eq!(typedef_basic.param_assigns().count(), 2);

    let typedef_extends = parse_and_find_node::<TypeDef>("typedef extended extends simple_signal { domain = digital; }");
    assert_eq!(typedef_extends.name().unwrap().text(), "extended");
    let base = typedef_extends.base_type().expect("Missing base type");
    let base_name_token = base.syntax().children_with_tokens()
                            .filter_map(|el| el.into_token())
                            .find(|tok| tok.kind() == SyntaxKind::IDENT);
    assert!(base_name_token.is_some());                       
    assert_eq!(base_name_token.unwrap().text(), "simple_signal");
    assert_eq!(typedef_extends.param_assigns().count(), 1);

    let typedef_extends_no_body = parse_and_find_node::<TypeDef>("typedef more_extended extends extended;");
    assert_eq!(typedef_extends_no_body.name().unwrap().text(), "more_extended");
    let base2 = typedef_extends_no_body.base_type().expect("Missing base type 2");
    let base2_name_token = base2.syntax().children_with_tokens()
                            .filter_map(|el| el.into_token())
                            .find(|tok| tok.kind() == SyntaxKind::IDENT);
    assert!(base2_name_token.is_some());                          
    assert_eq!(base2_name_token.unwrap().text(), "extended");
    assert_eq!(typedef_extends_no_body.param_assigns().count(), 0);
    assert!(!typedef_extends_no_body.syntax().children_with_tokens().any(|t| t.kind() == SyntaxKind::L_BRACE.into()));
    assert_eq!(typedef_extends_no_body.syntax().last_token().unwrap().kind(), SyntaxKind::SEMI.into());
}

#[test]
fn test_import_stmt() {
    let import_simple = parse_and_find_node::<ImportStmt>("import my.lib.ComponentA;");
    assert!(import_simple.path().is_some());
    assert!(import_simple.target().is_none(), "Simple import should have no target node");
    assert!(import_simple.alias().is_none());

    let import_group = parse_and_find_node::<ImportStmt>("import my.lib.{CompA, CompB};");
    assert!(import_group.path().is_some());
    match import_group.target() {
        Some(ImportTargetKind::Group(group)) => {
            assert_eq!(group.targets().count(), 2);
            let targets: Vec<_> = group.targets().collect();
            assert_eq!(targets[0].text(), "CompA");
            assert_eq!(targets[1].text(), "CompB");
        }
        _ => panic!("Expected group target"),
    }
    assert!(import_group.alias().is_none());

    let import_alias = parse_and_find_node::<ImportStmt>("import my.lib.CompC as C;");
    assert!(import_alias.path().is_some());
    assert!(import_alias.target().is_none(), "Alias import should have no target node");
    let alias = import_alias.alias().expect("Missing alias");
    assert_eq!(alias.name().unwrap().text(), "C");
}

// --- Test Port Decls with Interface Types ---

const PORT_TEST_SETUP: &str = "interface DummyInterface { pins { pin dummy_pin: signal; } }\n";

#[test]
fn test_port_decl_name_direction_type() {
    let text = format!("{} board B {{ ports {{ port data_in: DummyInterface; }} }}", PORT_TEST_SETUP);
    let port_decl = parse_and_find_node::<PortDecl>(&text);
    
    assert!(port_decl.name().is_some());
    assert_eq!(port_decl.name().unwrap().text(), "data_in");

    assert!(port_decl.type_ref().is_some());
    assert_eq!(port_decl.type_ref().unwrap().name_token().unwrap().text(), "DummyInterface");
}

#[test]
#[ignore = "Parser bug: Does not correctly parse bus suffix on port declarations"] 
fn test_port_decl_with_bus_range() {
    let text = format!("{} board B {{ ports {{ port DATA[7:0]: DummyInterface; }} }}", PORT_TEST_SETUP);
    let port_decl = parse_and_find_node::<PortDecl>(&text);

    assert_eq!(port_decl.name().unwrap().text(), "DATA");
    assert!(port_decl.bus_suffix().is_some());
    let suffix = port_decl.bus_suffix().unwrap();
    assert!(suffix.range().is_some());
    let range = suffix.range().unwrap();
    assert_eq!(range.lhs().unwrap().syntax().first_token().unwrap().text(), "7");
    assert_eq!(range.rhs().unwrap().syntax().first_token().unwrap().text(), "0");
    assert_eq!(range.separator_kind().unwrap(), SyntaxKind::COLON);
    assert!(port_decl.type_ref().is_some());
    assert_eq!(port_decl.type_ref().unwrap().name_token().unwrap().text(), "DummyInterface");
}

#[test]
#[ignore = "Parser bug: Does not correctly parse bus suffix on port declarations"] 
fn test_port_decl_with_bus_index() {
    let text = format!("{} board B {{ ports {{ port CS[0]: DummyInterface; }} }}", PORT_TEST_SETUP);
    let port_decl = parse_and_find_node::<PortDecl>(&text);
    
    assert_eq!(port_decl.name().unwrap().text(), "CS");
    assert!(port_decl.bus_suffix().is_some());
    let suffix = port_decl.bus_suffix().unwrap();
    assert!(suffix.index_expr_node().is_some());
    assert!(suffix.range().is_none());
    assert_eq!(suffix.index_expr_node().unwrap().text(), "0");
    assert!(port_decl.type_ref().is_some());
    assert_eq!(port_decl.type_ref().unwrap().name_token().unwrap().text(), "DummyInterface");
}
