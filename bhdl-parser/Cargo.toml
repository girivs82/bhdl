[package]
name = "bhdl-parser" # Correct crate name
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rowan = "0.15.15"
logos = "0.14.0"
smol_str = "0.2.1"
num-derive = "0.4.2"
num-traits = "0.2.19"

[dev-dependencies]
bhdl-ast = { path = "../bhdl-ast" }