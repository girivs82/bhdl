# IEC 62380 coefficient table for the bhdl reliability engine
# (docs/spec/Functional_Safety.md §2.8).
#
# ┌─────────────────────────────────────────────────────────────────┐
# │ FIXTURE — every number below is ILLUSTRATIVE, chosen only to    │
# │ exercise the pipeline. NOT transcribed from IEC TR 62380:2004.  │
# │ Replace each row with the standard's real constants (or your    │
# │ Isograph RWB configuration's equivalents) and update the row    │
# │ source before using any computed FIT in a real FMEDA.           │
# └─────────────────────────────────────────────────────────────────┘
#
# Model shape per class:  λ = λ_base · π_T · π_S
#   π_T = exp(Ea/k · (1/T_ref − 1/T_amb))       (temperatures in K)
#   π_S = (S / S_ref)^n,  S = applied/rated (sim-derived, never guessed)

standard = "IEC62380"
source = "FIXTURE — illustrative coefficients, IEC TR 62380:2004 constants NOT yet transcribed"

[classes.res_film]
lambda_base = 0.1   # FIT at reference conditions — FIXTURE
ea_ev = 0.15        # activation energy, eV — FIXTURE
t_ref_c = 40.0      # reference ambient, °C — FIXTURE
s_ref = 0.5         # reference stress ratio — FIXTURE
stress_exp = 1.0    # stress exponent — FIXTURE
source = "FIXTURE (shape: IEC 62380 fixed-film resistor model)"
