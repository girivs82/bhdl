[package]
name = "bhdl-stdlib"
version = "0.1.0"
edition = "2021"
authors = ["BHDL Team"]
description = "BHDL Standard Library - Component definitions and utilities"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
bhdl-parser = { path = "../bhdl-parser" }
bhdl-ast = { path = "../bhdl-ast" }
bhdl-netlist = { path = "../bhdl-netlist" }
bhdl-common = { path = "../bhdl-common" }
log = "0.4"

[dev-dependencies]