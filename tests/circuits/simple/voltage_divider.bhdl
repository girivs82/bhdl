// Voltage divider — two resistors, no nonlinear components
import { Res } from "bhdl-stdlib/simple/resistor_simple.bhdl";

board VoltageDivider {
    power VIN = 12V @ 100mA;
    ground GND;

    net divided: VIN -> r_top: Res(10000).1 -> r_top.2 -> r_bot: Res(10000).1;
    r_bot.2 -> GND;
}
