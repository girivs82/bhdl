// Minimal test to debug parser issues
board TestBoard {
    nets {
        signal;
        signal;
    }
    
    components {
        component TestComp U1;
    }
    
    connections {
        connect A -> U1.1;
        connect U1.2 -> B;
    }
}