// ELABORATED bhdl — generated from buck_converter_tps54302.bhdl; DO NOT EDIT.
// The round-trip gate re-synthesizes this file and requires the
// IDENTICAL netlist. Every synthesized element carries a
// provenance comment naming the intent that produced it.

// Buck Converter using TPS54302
// 12V to 5V @ 2A switching regulator

import { TPS54302 } from "bhdl-stdlib/power/tps54302.bhdl";
import { Capacitor } from "bhdl-stdlib/passives/capacitor.bhdl";
import { Resistor } from "bhdl-stdlib/passives/resistor.bhdl";
import { Inductor } from "bhdl-stdlib/passives/inductor.bhdl";

board BuckConverterTPS54302 {
    power VIN = 12V @ 3A;
    power VOUT = 5V @ 2A;
    ground GND;

    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    c_boot: Capacitor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    c_in1: Capacitor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    c_in2: Capacitor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    c_out1: Capacitor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    c_out2: Capacitor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    l1: Inductor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    r_fb1: Resistor();
    // WARNING: no ctor signature for this entity — emitted bare; round-trip unverified
    r_fb2: Resistor();
    u1: TPS54302();
    c_in1.2 -> @GND;
    c_in2.2 -> @GND;
    c_out1.2 -> @GND;
    c_out2.2 -> @GND;
    r_fb2.2 -> @GND;
    u1.GND -> @GND;
    @VIN -> c_in1.1;
    @VIN -> c_in2.1;
    @VIN -> u1.EN;
    @VIN -> u1.VIN;
    @VOUT -> c_out1.1;
    @VOUT -> c_out2.1;
    @VOUT -> l1.2;
    @VOUT -> r_fb1.1;
    c_boot.2 -> u1.PH;  // net auto_c_boot_2
    r_fb1.2 -> r_fb2.1;  // net auto_r_fb1_2
    r_fb1.2 -> u1.FB;  // net auto_r_fb1_2
    c_boot.1 -> u1.BOOT;  // net auto_u1_BOOT
    l1.1 -> u1.SW;  // net auto_u1_SW
}
