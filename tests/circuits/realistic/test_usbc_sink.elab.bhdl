// ELABORATED bhdl — generated from test_usbc_sink.bhdl; DO NOT EDIT.
// The round-trip gate re-synthesizes this file and requires the
// IDENTICAL netlist. Every synthesized element carries a
// provenance comment naming the intent that produced it.

// synthesized imports: expansion children reference these
import { USB_C_Receptacle_USB2_16P } from "bhdl-stdlib/connectors/usb_c.bhdl";

// USB-C 5 V sink power entry — the smallest board that is a CORRECT
// Type-C powered device: the USB_C_Sink5V composite (whose expansion
// carries the two independent 5.1 kΩ Rd pulldowns — without them a
// C-to-C cable never applies VBUS at all, and with one SHARED Rd an
// e-marked cable reads Debug/Audio-Accessory states and refuses
// power: AN1953 §3.2/§3.4, the Raspberry Pi 4 rev-1.0 field bug),
// bulk + HF decoupling, a power LED as the live load, and the D±
// pairs brought to a header.

import { USB_C_Sink5V } from "bhdl-stdlib/connectors/usb_c.bhdl";
import { Res } from "bhdl-stdlib/passives/resistor.bhdl";
import { Cap, ElectrolyticCap } from "bhdl-stdlib/passives/capacitor.bhdl";
import { LED } from "bhdl-stdlib/optoelectronic/led.bhdl";
import { Pin_Header_01x03 } from "bhdl-stdlib/connectors/pin_header.bhdl";

layout UsbcSink {
    layer_stackup 2;
    outline rect 30 22;
    place J_USB at (4, 11) rot 90;
    place J_DATA at (27.4, 11);
}

board UsbcSink {
    power VBUS = 5V @ 0.5A;
    ground GND;

    D_pwr: LED(green);
    J_DATA: Pin_Header_01x03();
    J_USB: USB_C_Sink5V();
    attribute J_USB.expansion_applied = "true";
    J_USB_j: USB_C_Receptacle_USB2_16P();
    attribute J_USB_j.expansion_parent = "J_USB";
    attribute J_USB_j.expansion_role = "shunt";
    attribute J_USB_j.vpin_parent = "J_USB";
    attribute J_USB_j.vpin_role = "shunt";
    J_USB_r_cc1: Res(5.1kΩ, 5%, 0.25W);
    attribute J_USB_r_cc1.expansion_parent = "J_USB";
    attribute J_USB_r_cc1.expansion_role = "shunt";
    attribute J_USB_r_cc1.vpin_parent = "J_USB";
    attribute J_USB_r_cc1.vpin_role = "shunt";
    J_USB_r_cc2: Res(5.1kΩ, 5%, 0.25W);
    attribute J_USB_r_cc2.expansion_parent = "J_USB";
    attribute J_USB_r_cc2.expansion_role = "shunt";
    attribute J_USB_r_cc2.vpin_parent = "J_USB";
    attribute J_USB_r_cc2.vpin_role = "shunt";
    c_bulk: ElectrolyticCap(10µF, 16V);
    c_hf: Cap(100nF, 50V);
    r_led: Res(330Ω, 5%, 0.25W);
    D_pwr.K -> @GND;
    J_DATA.3 -> @GND;
    J_USB.GND -> @GND;
    J_USB.SHIELD -> @GND;
    J_USB_j.A1 -> @GND;
    J_USB_j.A12 -> @GND;
    J_USB_j.B1 -> @GND;
    J_USB_j.B12 -> @GND;
    J_USB_j.S1 -> @GND;
    J_USB_r_cc1.2 -> @GND;
    J_USB_r_cc2.2 -> @GND;
    c_bulk.neg -> @GND;
    c_hf.2 -> @GND;
    J_USB_j.A5 -> J_USB_r_cc1.1;  // net J_USB_auto_10
    J_USB_j.B5 -> J_USB_r_cc2.1;  // net J_USB_auto_12
    J_DATA.2 -> J_USB.DM;  // net USB_DM
    J_DATA.2 -> J_USB_j.A7;  // net USB_DM
    J_DATA.2 -> J_USB_j.B7;  // net USB_DM
    J_DATA.1 -> J_USB.DP;  // net USB_DP
    J_DATA.1 -> J_USB_j.A6;  // net USB_DP
    J_DATA.1 -> J_USB_j.B6;  // net USB_DP
    @VBUS -> J_USB.VBUS;
    @VBUS -> J_USB_j.A4;
    @VBUS -> J_USB_j.A9;
    @VBUS -> J_USB_j.B4;
    @VBUS -> J_USB_j.B9;
    @VBUS -> c_bulk.pos;
    @VBUS -> c_hf.1;
    @VBUS -> r_led.1;
    D_pwr.A -> r_led.2;  // net auto_r_led_2
}
