# Marks tests/ as a library root so corpus boards can import
# test-support entities (lib_eff_reg.bhdl etc.) through the declared
# `tests` namespace in the repo-root bhdl.toml.
[library]
name = "tests"
version = "0.1.0"
